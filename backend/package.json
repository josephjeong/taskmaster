{
  "name": "tasker-backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.ts",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.12",
    "@types/node": "^15.12.4",
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.3.4"
  },
  "scripts": {
    "clean": "rimraf dist/*",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "tsc": "tsc",
    "build": "yarn-run-all clean lint tsc copy-assets",
    "dev": "nodemon --loader ts-node/esm index.ts",
    "start": "node --loader ts-node/esm index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
